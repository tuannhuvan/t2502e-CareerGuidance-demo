@model CareerGuidance.Models.CareerPath

@{
    ViewData["Title"] = "Delete Career Path";
}

<div class="container py-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <h3 class="mb-3">Delete Career Path</h3>
            <p class="text-danger mb-4">Are you sure you want to delete this career path?</p>

            <div class="card">
                <div class="card-body">
                    <p><strong>Title:</strong> @Model.Title</p>
                    <p><strong>Category ID:</strong> @Model.CategoryId</p>
                    <p><strong>Content:</strong> @Html.Raw(Model.Content)</p>
                </div>
                <div class="card-footer bg-light d-flex gap-2">
                    <form asp-action="Delete" method="post" class="d-inline">
                        <input type="hidden" name="id" value="@Model.Id">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                </div>
            </div>
        </div>
    </div>
</div>
